name: data
version: 0.0.1
publish_to: 'none'
homepage:

environment:
  sdk: '>=2.19.2 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  domain:
    path: ../domain
  shared_preferences: ^2.1.0
  firebase_auth: ^4.4.0
  logger: ^1.3.0
  cloud_firestore: ^4.8.1
  json_annotation: ^4.8.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner: ^2.3.3
  json_serializable: ^6.6.1
  
  